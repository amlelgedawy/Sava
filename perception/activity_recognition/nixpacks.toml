[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "xorg.libxcb"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/nix/store/*/lib waitress-serve --host 0.0.0.0 --port $PORT activity_server:app"
