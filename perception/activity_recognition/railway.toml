[build]
builder = "nixpacks"

[build.env]
NIXPACKS_PYTHON_VERSION = "3.11"
NIXPACKS_APT_PKGS = "libxcb1"

[deploy]
startCommand = "waitress-serve --host 0.0.0.0 --port $PORT activity_server:app"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
