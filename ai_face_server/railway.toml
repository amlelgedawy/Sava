[build]
builder = "nixpacks"

[build.env]
NIXPACKS_PYTHON_VERSION = "3.11"
NIXPACKS_DEBIAN_PKGS = "libxcb1"

[deploy]
startCommand = "uvicorn ai_face_server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
