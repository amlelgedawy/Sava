[phases.setup]
nixPkgs = ["python311", "xorg.libxcb"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn ai_face_server:app --host 0.0.0.0 --port $PORT"
